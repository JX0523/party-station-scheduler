# 开发日志 — 2026-08-21

## 事件

### Supabase 再次警告「低活跃度」⚠️

- **现象**：收到 Supabase 邮件，项目（`mkbcbfzfrdrqywzjbrbu`）已超过 7 天「没有足够活动」，即将被暂停
- **排查**：keep-alive workflow 一直在正常跑（08-12 / 08-16 / 08-19 三次 schedule 全部 success，HTTP 200）
- **根因**：Supabase 的活动检测基于**数据库活动**，SELECT 查询可能被缓存命中而不计入；
  REST 层返回 200 不代表数据库计时器被重置。需要**真实写操作（INSERT）**
- **教训**：workflow 成功 ≠ 保活有效。验证手段必须与被检测的指标一致

## 修复：保活机制 v5 升级

### 1. 新增心跳表（[migration-v5-keepalive.sql](database/migration-v5-keepalive.sql)）
- `keep_alive_pings` 表：id、pinged_at、source
- RLS 允许 anon 角色 INSERT/DELETE（表内无敏感数据）
- 同步更新 [schema.sql](database/schema.sql)（第 8 节）

### 2. 保活方式改造（[keep-alive.yml](.github/workflows/keep-alive.yml)）
| 维度 | 之前（v4） | 之后（v5） |
|------|-----------|-----------|
| 操作类型 | SELECT 查询 | **INSERT 真实写操作** |
| 频率 | 每周 2 次（周日+周三） | **每天 1 次** |
| 触发时间 | UTC 03:07 | UTC 03:17（北京时间 11:17） |
| 数据清理 | 无 | 自动删除 30 天前旧心跳 |

### 3. 文档更新
- [tech-spec.md](docs/tech-spec.md) 第 7 节变更记录登记
- [CHANGELOG.md](CHANGELOG.md) 追加 2026-08-21 条目

## 执行步骤（迁移）

- [x] 代码与文档提交推送（commit `bc67490`）
- [x] **Supabase SQL Editor 执行 migration-v5-keepalive.sql**（2026-08-21 完成）
- [x] 手动触发 Keep Supabase Alive 验证：INSERT HTTP 201 ✅ + Cleanup HTTP 204 ✅（run 32444240767）
- [x] 已确认 schedule 配置为每天 UTC 03:17，明日自动执行

## 备注

- Supabase 官方文档：免费项目 7 天不活跃会被暂停，「每天几条数据库请求」即可保持活跃
- 付费方案（Pro $25/月）无暂停限制，是彻底解决方案，但当前免费方案下 v5 机制已足够

---

## 追加：Netlify 构建积分告警处理（2026-08-21 晚）

### 事件
- Netlify 邮件提醒：团队 JX0523 本周期（7/26~8/25）300 构建积分已用 75%
- 排查：部署工作流每次 push 都触发构建，纯文档提交也在烧积分；
  且 Netlify 后台可能连接了 GitHub 自动构建（与 Actions 双倍构建）

### 处理
1. **deploy.yml 加 paths 过滤**（commit 484aa60）：只有 frontend/** 或工作流本身变更才部署
2. **新增 netlify-autofix.yml**（commit 4a95457）：自动调用 Netlify API 诊断并关闭
   Netlify GitHub 自动构建（stop_builds=true，可逆）；已运行 3 次全部 success
3. 后续：若自动构建确实存在 → 已关闭；积分消耗应大幅下降，8/25 重置后观察 Usage 页

### 说明
- GitHub 日志/页面读取需要登录，国内网络访问 GitHub 不稳定（git 通道正常，网页/API 时通时断）
- 自动修复工作流附带"结果写 Issue"步骤（无权限时静默跳过，不影响主流程）
---

## 追加2：Netlify 自动构建确认关闭 + 部署流水线测试（2026-08-21 深夜）

### 确认
- 收到 Netlify 官方通知：**builds 已停止**（由账号"佳欣 潘"操作，即自动修复工作流用 API 执行的结果）
- 含义：Netlify 不再自动构建；网站更新完全走 GitHub Actions 构建 + API 上传（免费，不受影响）
- 已验证：两个网站均 HTTP 200

### 部署流水线测试（stop-builds 之后首次前端部署）
- 改动：frontend/index.html 增加 description meta（无害小改进，同时作为流水线验证标记）
- 预期：push 后 Deploy 运行 success，且线上 index.html 出现该 meta

### 新约定：每次操作后的标准流程（用户明确要求，勿再让用户重复）
1. 检查（测试 254 项 + 构建 + 改动文件 lint 清零）
2. 写文档（dev-log 当日 + CHANGELOG + 相关规范）
3. 上传（commit + push）
4. 验证（Deploy 结论 + 网站 HTTP 200 + 产物更新）
- 已写入 CLAUDE.md「⭐ 每次操作完成后的标准流程」，后续所有改动自动执行


